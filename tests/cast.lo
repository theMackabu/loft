fn main() {
   let unit = ();
   let single_tuple: (i32,) = (1,);
   let tuple: (i32, &str) = (42, "hello");
   
   test_eq!(unit, ());
   test_eq!(single_tuple, (1));
   test_eq!(tuple, (42, "hello"));
}