// macro_rules! add {
//    ($a:expr, $b:expr) => {
//       $a + $b
//    };
// }

macro_rules! tuple {
   ($($item:expr),*) => (($($item),*));
}

fn main() {   
   // let val = add!(5, 3);
   // io::println(val);
   
   let tpl = tuple!(1, "hello", true);
   io::println(tpl);
}